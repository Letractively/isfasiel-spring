<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="path">
	<typeAlias alias="hashMap" type="java.util.HashMap"/>

	<select id="pathDAO.list" resultClass="hashMap">
		SELECT PATH_ID, PATH_NAME FROM TN_PATH
		WHERE STATE = 0 AND DEL_YN = 'N'
		ORDER BY PATH_ID ASC 
	</select>
	<delete id="pathDAO.delete">
		DELETE FROM TN_CNT_PATH
		WHERE CONTENT_ID = #contentId#
	</delete>
	<insert id="pathDAO.insert">
		INSERT TN_CNT_PATH 
			(
				CONTENT_ID
				, PATH_ID
				, USER_IDX
			) 
		VALUES (
			#contentId#
			, #pathId#
			, #userIdx#
		)
	</insert>
</sqlMap>