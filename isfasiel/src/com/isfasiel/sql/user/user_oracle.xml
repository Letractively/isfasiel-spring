<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE sqlMap      
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="user">
	<typeAlias alias="hashMap" type="java.util.HashMap"/>
	
	<typeAlias alias="user" type="com.isfasiel.main.domain.User"/>

	<insert id="userDAO.createUser" parameterClass="user">
		INSERT INTO TN_USER_INFO (USER_IDX, USER_EMAIL, PASSWORD) VALUES (#id#, #email#, #password#)
	</insert>
	<select id="userDAO.findUser" resultClass="user">
		SELECT USER_IDX AS id, USER_EMAIL, PASSWORD FROM TN_USER_INFO WHERE USER_EMAIL = #email# ORDER BY ID
	</select>
	<select id="userDAO.login" resultClass="user">
		SELECT USER_IDX AS ID, USER_EMAIL, PASSWORD FROM TN_USER_INFO
		WHERE USER_EMAIL = #email# AND PASSWORD = #password#
	</select>
</sqlMap>